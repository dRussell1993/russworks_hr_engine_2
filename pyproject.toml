[project]
name = "russworks-hr-engine"
version = "1.0.0"
description = "Russ-Works MLB HR scoring and report engine"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
russworks = "russworks.main:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
